<div class="max-w-4xl mx-auto p-6 bg-gray-100 rounded-lg shadow-md">
  <h1 class="text-2xl font-bold mb-6 text-gray-900">Editing Post</h1>

  <%= render "form", post: @post %>

  <div class="mt-6 flex justify-between items-center">
    <%= link_to "Show this post", @post, class: "text-blue-500 hover:text-blue-700 font-semibold" %>
    <%= link_to "Back to posts", posts_path, class: "text-blue-500 hover:text-blue-700 font-semibold" %>
  </div>
</div>
